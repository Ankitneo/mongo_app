<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @article.name %>
</p>
<p>
  <strong>Content:</strong>
  <%= @article.content %>
  <% @article.image_file_name %>
  <br>
  <%  if @article.image_file_name.nil? %>
  <% else %>
 <% @url= image_url @article.image %>
  <%= image_tag @url %>
  <% end %>
</p>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>
